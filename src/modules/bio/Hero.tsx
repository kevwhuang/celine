import React from 'react';

import '../../styles/modules/bio/Hero.scss';

function Hero(): React.ReactElement {
    return (
        <section className="hero">
        </section>
    );
}

export default Hero;
