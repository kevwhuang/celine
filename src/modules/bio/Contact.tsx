import React from 'react';

import '../../styles/modules/bio/Contact.scss';

function Contact(): React.ReactElement {
    return (
        <section className="contact" id="contact" />
    );
}

export default Contact;
