import React from 'react';

import '../../styles/modules/bio/Credentials.scss';

function Credentials(): React.ReactElement {
    return (
        <section className="credentials" />
    );
}

export default Credentials;
